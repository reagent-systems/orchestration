# ADK Framework (with built-in Google Search)
google-adk==1.4.2

# Environment and configuration
python-dotenv>=1.0.0

# Async operations
# asyncio is built-in Python module

# Data handling (for workspace results)
# pathlib is built-in Python module 
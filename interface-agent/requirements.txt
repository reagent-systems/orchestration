# Interface Agent Requirements
# Human-friendly CLI interface for orchestration system

# Core dependencies (shared with other agents)
flask>=2.3.0
pydantic>=2.0.0
python-dotenv>=1.0.0
google-adk==1.4.2
requests>=2.31.0

# CLI and interface specific
click>=8.0.0
rich>=13.0.0
prompt-toolkit>=3.0.0
colorama>=0.4.6  # Cross-platform colored terminal text

# Standard library dependencies (already included in Python)
# json, os, sys, time, datetime, pathlib, subprocess 

# ADK Framework (with built-in tools)
google-adk==1.4.2

# Environment and configuration
python-dotenv>=1.0.0

# Textual TUI Framework
textual>=0.47.0
rich>=13.0.0

# File watching
watchdog>=3.0.0

# Built-in modules (no need to install):
# asyncio, pathlib, json, typing 